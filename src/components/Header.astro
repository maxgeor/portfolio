---
import Link from "./Link.astro"
import Logo from "./Logo.astro";
import CopyEmailButton from "./CopyEmailButton.astro";

export interface Props {
  goBackPath?: string;
  classes?: string;
}

const { classes = "" } = Astro.props;

const currentPath = Astro.url.pathname;
---
<header class={`col-span-full grid grid-cols-12 gap-4 ${classes}`}>
  <a href="/" class="col-span-4 sm:col-span-2">mkcg</a>

  <p class="col-span-6 sm:col-span-8 ">
    <a href="/">Work</a>, <a href="/writing">Writing</a>
  </p>
  <a href="/info" class={`col-span-2 justify-self-end`}>Info</a>
</header>