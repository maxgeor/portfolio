<button id="theme-button" class="group justify-self-end p-5 -m-5">
  <div class="h-2 w-2 rounded-full group-hover:scale-[120%] group-active:scale-[110%] border border-black dark:border-[#9ea1a3] group-hover:bg-[#1c1e1e] group-hover:border-[#1c1e1e] dark:group-hover:bg-white dark:group-hover:border-white shrink-0 transition duraton-75"></div>
</button>

<script>
  const themeButton = document.querySelector('#theme-button') as HTMLButtonElement;

  const toggleTheme = () => {
    switch (localStorage.theme) {
      case "light":
        localStorage.theme = 'dark'
        break;
      case "dark":
        localStorage.theme = 'light'
        break;
      default:
        localStorage.theme = 'light'
    }
    location.reload();
  };

  themeButton.addEventListener('click', () => toggleTheme());
</script>
