---
export interface Props {
	type: 'project' | 'writing';
	href: string;
	title: string;
	description: string;
	externalLink?: boolean;
}

const { type, href, title, description, externalLink = false } = Astro.props;
---
<a 
	href={href}
	target={externalLink ? '_blank' : '_self'}
	class={`
	group relative col-span-full items-baseline gap-x-6 sm:gap-x-12 flex
	`}
>
	<p class='basis-1/2 group-hover:underline'>
		{title}
	</p>
	<p class={`
		${type === 'writing' ? 'text-[13px]' : null}
		basis-1/2
	`}>
		{description}
	</p>
</a>
