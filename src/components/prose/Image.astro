---
export interface Props {
  src: string;
  alt: string;
  classes?: string;
}
const { src, alt, classes = "" } = Astro.props;
---
<figure class={`relative h-0 w-full pt-[calc(4/7*100%)] order-last col-span-full xl:col-start-2 xl:col-end-8 select-none	border border-neutral-150/[65%] dark:border-0 dark:ring-1 dark:ring-white dark:ring-inset rounded-lg bg-neutral-50 dark:bg-neutral-900 ${classes}`}>
  <img
    src={src}
    alt={alt}
    class={`aspect-[7/4] absolute inset-0 h-full w-full object-cover rounded-[0.45rem] dark:brightness-90`}
  />
</figure>
