---
export interface Props {
  src: string;
  alt: string;
  classes?: string;
}
const { src, alt, classes = '' } = Astro.props;
---
<img
  src={src} 
  alt={alt} 
  class={`
    bg-gray-50 
    ${classes}
  `} 
/>