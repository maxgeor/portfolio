---
export interface Props {
  src: string;
  alt: string;
  isPadded?: boolean;
  classes?: string;
}
const { src, alt, isPadded = false, classes = '' } = Astro.props;
---
<img
  src={src} 
  alt={alt} 
  class={`
    bg-gray-50 h-full
    ${classes}
  `} 
/>