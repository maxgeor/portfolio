---
import { marked } from 'marked';
import Image from './Image.astro';

export interface Props {
  title: string;
  image?: {
    src: string;
    alt: string;
    id: string;
    hasBorder?: boolean;
  }
}

const { title, image } = Astro.props;
---

<div class="col-span-full md:col-span-9 md:col-start-3 flex flex-col lg:flex-row gap-y-15 gap-x-5">
  <div class="col-span-full  flex flex-col  lg:basis-1/4 lg:order-last">
    <h3 class="text-pretty uppercase" set:html={marked.parse(title)}></h3>
    <div class=" flex flex-col gap-2 text-balance  normal-case">
      <slot />
    </div>
  </div>
  {image && (
    <Image
      id='T1'
      src={image.src}
      alt={image.src}
      span={{ base: 12 }}
      hasCaption={false}
      hasBorder={image.hasBorder}
      classes="self-center lg:basis-3/4 "
    />
  )}
</div>
