---
import { marked } from 'marked';
import Image from './Image.astro';

export interface Props {
  title: string;
  image?: {
    src: string;
    alt: string;
    id: string;
  }
}

const { title, image } = Astro.props;
---

<div class="col-span-full md:col-span-8 md:col-start-3 flex flex-col gap-3">
  <div class="col-span-full flex flex-col  ">
    <h3 class="border-b border-neutral-100" set:html={marked.parse(title)}></h3>
    <div class=" flex flex-col gap-2 max-w-xl">
      <slot />
    </div>
  </div>
  {image && (
    <Image
      id='T1'
      src={image.src}
      alt={image.src}
      span={{ base: 12, xl: 6 }}
      hasCaption={false}
      classes="self-center"
    />
  )}
</div>
