---
import { marked } from 'marked';
import Image from './Image.astro';

export interface Props {
  title: string;
  image?: {
    src: string;
    alt: string;
    id: string;
  }
}

const { title, image } = Astro.props;
---

<div class="col-span-full md:col-span-6 md:col-start-4 flex flex-col gap-3">
  <div class="col-span-full flex flex-col   gap-1">
    <h3 set:html={marked.parse(title)}></h3>
    <div class=" flex flex-col gap-2 text-balance font-medium text-[12px] normal-case">
      <slot />
    </div>
  </div>
  {image && (
    <Image
      id='T1'
      src={image.src}
      alt={image.src}
      span={{ base: 12, xl: 6 }}
      hasCaption={false}
      classes="self-center"
    />
  )}
</div>
