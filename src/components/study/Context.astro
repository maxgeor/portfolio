---
export interface Props {
  title?: string
}

const { title } = Astro.props;
---
<div class="grid grid-cols-16 !col-span-full gap-y-10 shadow-sub-pixel-black dark:shadow-sub-pixel-white pt-15 lg:pt-30 mt-15 lg:mt-[30px] first:mt-0 lg:first:mt-0" >
<!-- <div class="grid grid-cols-16 !col-span-full gap-y-10 mt-30" > -->
  {title && <h1 class="text-pretty col-span-full lg:col-span-2 lg:pr-30">{title}</h1>}
  <p class="col-span-full lg:col-span-12 break-words hyphens-auto tracking-[-0.0075em] text-[19px] leading-[20px] lg:text-[24px] lg:leading-[25px] 2xl:text-[30px] 2xl:leading-[30px] 3xl:text-[39px] 3xl:leading-[40px]">
    <slot />
  </p>
</div>

<style is:inline>
  p > mark {
    background-color: transparent;
    color: inherit;
  }

  p > mark.solution::before {
    content: '';
    display: inline-block;
    height: 25px;
    width: 25px;
    margin-right: 9px;
    vertical-align: baseline;
    background-color: var(--accent-color);
  }

  @media (max-width: 1599px) {
    p > mark.solution::before {
      height: 17px;
      width: 17px;
      margin-right: 6px;
    }
  }

  @media (max-width: 1023px) {
    p > mark.solution::before {
      height: 14px;
      width: 14px;
      margin-right: 5px;
    }
  }
</style>
