---
export interface Props {
  classes?: string;
}
const { classes = '' } = Astro.props;
---

<section class={`
  col-span-full xl:grid-span-8 xl:col-start-2 grid grid-cols-6 xl:grid-cols-8 gap-y-[5px]6 gap-x-[15px]
  ${classes}
`}>
  <slot />
</section>