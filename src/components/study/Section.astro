---
import Caption from './Caption.astro';
import clsx from 'clsx';

export interface Props {
  id: string;
  title: string
  classes?: string;
}

const { id, title, classes = "" } = Astro.props;
---
<section 
  id={id + '-' + title} 
  aria-label={title}
  class={clsx("scroll-mt-[43px] md:scroll-mt-[50px] relative grid grid-cols-12 gap-x-4 col-span-full lg:mt-8", classes)}
>
  <div class="grid grid-cols-12 gap-x-4 gap-2 md:hidden sticky top-[41px] col-span-full z-[100] mix-blend-difference border-t-[0.5px] border-white">
    <Caption element="h2" id={id} text={title} classes='text-pretty text-white mt-1.5' />
  </div>
  <hr class="not-prose border-0 border-b-[0.5px] hidden md:block col-span-full mb-1.5 border-black dark:border-white" />
  <Caption element="h2" id={id} text={title} classes='hidden md:flex sticky top-[48px] uppercase text-pretty col-span-2 gap-2 z-[100] mix-blend-difference text-white' />
  <div class="context flex flex-col -my-0.5 lg:-mt-4 [&_mark]:bg-transparent [&_mark]:text-black dark:[&_mark]:text-white [&_mark]:font-bold col-span-full  lg:col-start-3 lg:col-span-6 2xl:col-span-4 xl:col-start-3 2xl:col-start-3 h-fit [&>p]:mb-3 last:[&>p]:mb-0 pt-3.5 lg:pt-0 [&_p]:max-w-[85ch] ">
    <slot name="context" />
  </div>
  {Astro.slots.has('default') && (
    <div class="grid grid-cols-12 gap-y-8 mt-8 col-span-full">
      <slot />
    </div>
  )}
</section>

<style>
  section::after {
    content: '';
    display: block;
    height: 1px;
    margin-bottom: -1px;
  }

  section h2::before,
  section div:first-child::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    backdrop-filter: invert(1);
    z-index: -1;
    transition: opacity 0.3s ease;
    opacity: 0;
  }
</style>