---
import { formatDate } from '../utils/formatting';

export interface Props {
  title: string;
  href: string;
  date: string;
}

const { title, href, date } = Astro.props;

const formattedDate = formatDate(date);
---

<a 
  href={href}
  class="col-span-full hover:bg-white hover:text-black flex justify-between xs:justify-auto gap-4 border-t  border-neutral-600 last:border-y"
>
  <span class="xs:basis-1/2 uppercase">{title}</span>
  <span class="xs:basis-1/2">{formattedDate}</span>
</a>