---
export interface Props {
  title?: string;
  description?: string;
  maxWidth?: string | null;
  backgroundColor?: string;
}

const {
  title = "Max Georgopoulos",
  description,
  maxWidth = "",
  backgroundColor = "bg-[#fff]",
} = Astro.props;
---

<!DOCTYPE html>
<html lang="en" class="scroll-smooth ">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    <meta name="description" content={description || "Designer & developer"} />
  </head>
  <body
    style="text-rendering: optimizeLegibility;"
    class={`
      ${backgroundColor} ${maxWidth}
     flex flex-col   gap-4 text-[#000] normal-case  min-h-screen text-[12px] leading-4 font-medium selection:bg-[#e1e1e1] antialiased p-4 pb-8 pt-10  h-min  mx-auto
    `}
  >
    <slot />
  
  </body>
</html>

<style is:global>  
  .tabualar {
    font-variant: small-caps;
  }
  .text-balance {
    text-wrap: balance;
  }
  .small-caps {
    font-variant: small-caps;
  }
  
  @font-face {
    font-family: Romie;
    src: url(/fonts/RomieTrial-Regular-Italic.otf);
  }
  
  @font-face {
    font-family: London;
    src: url(/fonts/london.ttf);
  }
  
  @font-face {
    font-family: Pixel;
    src: url(/fonts/MintsodaLimeGreen.ttf);
  }
  
  @font-face {
    font-family: Gothic;
    src: url(/fonts/gothic.ttf);
  }
  
  @font-face {
    font-family: Geist Mono;
    src: url(/fonts/geist-mono/GeistMono-Regular.woff2);
  }

  @font-face {
    font-family: Romie;
    src: url(/fonts/romie/RomieTrial-Regular-Italic.otf);
  }
  
  @font-face {
    font-family: untitled;
    src: url(/fonts/untitled/test-untitled-sans-regular.woff2) format("woff2");
    font-weight: 400;
  }

  @font-face {
    font-family: untitled;
    src: url(/fonts/untitled/test-untitled-sans-medium.woff2) format("woff2");
    font-weight: 500;
  }

  @font-face {
    font-family: untitled;
    src: url(/fonts/untitled/test-untitled-sans-regular-italic.woff2)
      format("woff2");
    font-style: italic;
  }
  
  @font-face {
    font-family: untitled;
    src: url(/fonts/untitled/test-untitled-sans-medium-italic.woff2)
      format("woff2");
    font-weight: 500;
    font-style: italic;
  }

  @font-face {
    font-family: "Berkeley Mono";
    src: url(/fonts/BerkeleyMono-Regular.ttf);
  }

  html {
    text-rendering: optimizeLegibility;
    -webkit-tap-highlight-color: #e1e1e1;
  }
  a {
    text-underline-offset: 2px;
  }
  
  a:focus-visible {
    outline: solid 2px green;
    outline-offset: 1px;
    border-radius: 2px;
  }

  tr:last-child > td {
    padding-bottom: 0;
  }

  td {
    vertical-align: top;
  }

  del {
    text-decoration-thickness: 0.08em;
  }

  sup {
    margin-left: 1px;
  }

  select::-ms-expand {
    display: none;
  } 

  .no-scrollbar::-webkit-scrollbar {
    display: none;
  }

  .no-scrollbar {
    -ms-overflow-style: none;
    scrollbar-width: none;
  }

</style>
